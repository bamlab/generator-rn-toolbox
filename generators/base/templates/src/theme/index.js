// @flow

const gridSize = 8;

export default {
  fonts: {
    header: {
      fontSize: 20,
      fontWeight: 'bold',
    },
    default: {
      fontSize: 16,
    }
  },
  colors: {
    oceanBlue: '#05A5D1',
    darkGray: '#333333',
  },
  grid: {
    x1: gridSize * 1,
    x2: gridSize * 2,
    x3: gridSize * 3,
    x4: gridSize * 4,
    x5: gridSize * 5,
    x6: gridSize * 6,
    x8: gridSize * 8,
    x9: gridSize * 9,
    x10: gridSize * 10,
    x12: gridSize * 12,
    x14: gridSize * 14,
    x16: gridSize * 16,
  },
  dimensions: {
    touchableHeight: 48,
    visibleButtonHeight: 36,
  },
};
