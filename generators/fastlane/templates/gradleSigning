    signingConfigs {
        release {
            storeFile file(System.getenv("GRADLE_KEYSTORE_PATH"))
            storePassword System.getenv("GRADLE_KEYSTORE_PASSWORD")
            keyAlias System.getenv("GRADLE_KEYSTORE_ALIAS")
            keyPassword System.getenv("GRADLE_KEYSTORE_PASSWORD")
        }
    }